import React from 'react';

const AnswerCellItem = (ans, truth) => (
  <span
    style={{
      width: '100%',
      height: '100%',
      backgroundColor: '#dada',
    }}
  >
    {ans}
  </span>
);

export default AnswerCellItem;
